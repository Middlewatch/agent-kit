{
  "name": "pi-tui",
  "version": "0.0.1",
  "description": "Owner-fit pi TUI: gutter tool rendering, labeled footer, fade-glyph header, todo widget, and /edit into neovim.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": ">=0.80 <1.0",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "@types/node": "^26.0.1"
  }
}
