# Notation fixture 🚀

The build “succeeded” on the second try, and the log calls it ‘done’.

- ✅ the checklist bullet carries a decorative mark

The code span `it's "quoted" here 🎉` must stay excluded, and so must the
fence below.

```
curly “quotes” and a 🎯 inside a fence
```
