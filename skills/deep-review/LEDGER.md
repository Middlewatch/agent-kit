# deep-review — ledger

Revision record per AUTHORING.md's ledger section.

## 2026-09-03 — severity register and earned criteria moved in from the retired charter
- Motivation: the skill cited a severity register "defined in
  `guidance/workflows/freezable-workflow.md`" that no document in the estate
  defined; the charter itself was opened by four sessions in a month, all to
  edit it, none to apply it, while its six incident-backed review rules had
  no other home. Delegation doctrine likewise moved into the agent-delegate
  README when `delegation-standard.md` retired (two readers since its draft).
- Change: CRITICAL/MAJOR/MINOR/NOTE defined inline; five earned criteria
  added after the interrogation axes; delegation citation repointed to the
  extension README.
- Outcome (2026-09-04 sweep): no evidence either way; no review session ran
  between the edit and the sweep (the only severity-term hits are the 09-03
  sweep session itself).

## 2026-09-17 — full rewrite: procedure only, evidence moved to references
- Motivation: 349 lines, each step carrying its justification paragraph;
  the five change-review criteria and the refuter verdict schema sat inline
  though both are conditional. The skill had survived the skill-author and
  guidance passes without a structural edit (owner request, this session).
- Change: body cut to the nine steps plus output, each step stated as its
  action; step numbers preserved so `tool-gate-incidents.md` still cites
  step 8. Verdict schema moved to `references/verdict.schema.json` (delegate
  subset, no `$schema`), the five criteria to
  `references/change-review-criteria.md` with their incidents. Withdrawal
  rule folded into step 6; harness-neutral wording kept.
- Outcome (pending sweep)
