# Zig build output and cache — regenerated by `zig build`.
zig-out/
.zig-cache/

# Scratch, phase documents, and throwaway work.
artifacts/
notes/
scratch/
*.log

# Secrets and local overrides.
.env
*.local
