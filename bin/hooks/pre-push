#!/usr/bin/env bash
# Refuse a push whose tree fails the path lint or the owner-pattern scan.
# Enable with: git config core.hooksPath bin/hooks
set -euo pipefail
cd "$(git rev-parse --show-toplevel)"
bin/check --lint
bin/check --scan .
